# Plate frontend

Angular 22 PWA. See the repository root `README.md` and `INSTRUCTIONS.md`.

```powershell
npm start
```

Dev server: http://localhost:4200 (proxies `/api` to the Spring Boot app on port 8080).
